import {
  FileText,
  Hash,
  Search,
  Image,
  Calendar,
  Brain,
} from "lucide-react";

const features = [
  {
    icon: FileText,
    title: "AI Script Writer",
    description: "Generate engaging YouTube and Shorts scripts in seconds.",
  },
  {
    icon: Hash,
    title: "Hashtag Generator",
    description: "Create trending hashtags for maximum reach.",
  },
  {
    icon: Search,
    title: "SEO Optimizer",
    description: "Generate SEO-friendly titles and descriptions.",
  },
  {
    icon: Image,
    title: "Thumbnail Ideas",
    description: "Get creative thumbnail concepts that attract clicks.",
  },
  {
    icon: Calendar,
    title: "Content Planner",
    description: "Plan and organize your content calendar.",
  },
  {
    icon: Brain,
    title: "AI Assistant",
    description: "Let AI help you brainstorm fresh content ideas.",
  },
];

function Features() {
  return (
    <section className="py-24 px-6 bg-gray-900">
      <div className="max-w-7xl mx-auto">
        <h2 className="text-4xl font-bold text-center mb-4">
          Powerful AI Tools
        </h2>

        <p className="text-center text-gray-400 mb-16">
          Everything creators need in one platform.
        </p>

        <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {features.map((feature, index) => {
            const Icon = feature.icon;

            return (
              <div
                key={index}
                className="bg-gray-800 rounded-2xl p-8 hover:border hover:border-purple-500 transition"
              >
                <Icon className="text-purple-500 mb-6" size={36} />

                <h3 className="text-2xl font-semibold mb-3">
                  {feature.title}
                </h3>

                <p className="text-gray-400">
                  {feature.description}
                </p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}

export default Features;