<div className="bg-slate-900 rounded-xl p-8 shadow-lg">

  <h1 className="text-3xl font-bold text-white mb-6">
    ✨ AI Reel Script Generator
  </h1>

  <div className="grid grid-cols-2 gap-6">

    <div>
      <label className="text-gray-300">Topic</label>

      <input
        type="text"
        placeholder="Top 5 AI Tools..."
        value={topic}
        onChange={(e) => setTopic(e.target.value)}
        className="w-full mt-2 p-3 rounded-lg bg-slate-800 text-white outline-none"
      />
    </div>

    <div>
      <label className="text-gray-300">Platform</label>

      <select
        className="w-full mt-2 p-3 rounded-lg bg-slate-800 text-white"
      >
        <option>Instagram Reels</option>
        <option>YouTube Shorts</option>
        <option>TikTok</option>
      </select>
    </div>

    <div>
      <label className="text-gray-300">Duration</label>

      <select
        className="w-full mt-2 p-3 rounded-lg bg-slate-800 text-white"
      >
        <option>30 seconds</option>
        <option>60 seconds</option>
      </select>
    </div>

    <div>
      <label className="text-gray-300">Tone</label>

      <select
        className="w-full mt-2 p-3 rounded-lg bg-slate-800 text-white"
      >
        <option>Motivational</option>
        <option>Funny</option>
        <option>Professional</option>
        <option>Educational</option>
      </select>
    </div>

  </div>

  <button
    onClick={generate}
    className="mt-8 w-full bg-gradient-to-r from-violet-600 to-purple-700 hover:from-violet-700 hover:to-purple-800 py-4 rounded-xl text-white text-lg font-semibold transition-all"
  >
    {loading ? "Generating..." : "✨ Generate Script"}
  </button>

</div>